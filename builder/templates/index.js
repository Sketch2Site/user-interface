const { getCover } = require("./cover");
const { getLeftHero } = require("./leftHero");
const { getRightHero } = require("./rightHero");
const { getNavbar } = require("./navbar");
const { getParagraph } = require("./para");
const { putIntoLayout } = require("./layout");

module.exports = {
  getCover,
  getLeftHero,
  getRightHero,
  getNavbar,
  getParagraph,
  putIntoLayout,
};
